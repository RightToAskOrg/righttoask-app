<?xml version="1.0" encoding="utf-8"?>

<Frame
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:RightToAskClient.Views;assembly=RightToAskClient"
    xmlns:extensions="http://xamarin.com/schemas/2020/toolkit"
    x:Class="RightToAskClient.Views.LabeledPicker"
    CornerRadius="5"
    BorderColor="{AppThemeBinding Light={StaticResource FadedButtonColor}, Dark={StaticResource SeparatorDarkModeColor}}"
    Padding="16"
    BackgroundColor="{AppThemeBinding Light=White, Dark=Black}"
    x:Name="This"
    Margin="0 ,8"
    HasShadow="False">
    <StackLayout Orientation="Vertical"
                 VerticalOptions="Start"
                 Padding="0">
        <Label Text="{extensions:Translate SharingElectorateInfoPickerTitle}"
               Style="{StaticResource PickerTitle}"
               IsVisible="{Binding AbleToFinish}">
        </Label>
        <Grid Margin="0,0,0,16">
            <BoxView Style="{StaticResource Separator}" VerticalOptions="End" />

            <Image Source="{extensions:ImageResource RightToAskClient.Images.expand_48.png }"
                   extensions:IconTintColorEffect.TintColor="{AppThemeBinding Light={StaticResource TextColor} ,Dark={StaticResource TextColorDark}}"
                   HorizontalOptions="End" VerticalOptions="Center" WidthRequest="10" HeightRequest="10" />

            <Picker Title="{extensions:Translate SharingElectorateInfoPickerTitle}"
                    ItemsSource="{Binding SharingElectorateInfoOptionValues}"
                    Style="{StaticResource TransparentBgPicker}" HorizontalOptions="FillAndExpand"
                    SelectedIndex="{Binding SelectedIndexValue}" VerticalOptions="Center" />
        </Grid>
    </StackLayout>
</Frame>