<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:RightToAskClient.Views;assembly=RightToAskClient"
             xmlns:rightToAskClient="clr-namespace:RightToAskClient;assembly=RightToAskClient"
             xmlns:controls="clr-namespace:RightToAskClient.Controls;assembly=RightToAskClient"
             xmlns:models="clr-namespace:RightToAskClient.Models;assembly=RightToAskClient"
             xmlns:vm="clr-namespace:RightToAskClient.ViewModels"
             x:Class="RightToAskClient.Views.QuestionDetailPage"
             x:DataType="vm:QuestionViewModel">
    <ContentPage.Content>
		<StackLayout x:Name="WholePage" Margin="20">
			<Label Text="Question:" />
			<Label x:Name="QuestionDetailView" Text="{Binding Question.QuestionText}" FontSize="Large" FontAttributes="Bold"/>
			<Label Text="Question writer:" />
			<Label x:Name="Suggester" Text="{Binding Question.QuestionSuggester, StringFormat='{0:F0}'}" FontSize="Medium" />
			<Label Text="Upvotes:" />
			<StackLayout  Orientation="Horizontal" HorizontalOptions="FillAndExpand" >
				<Label x:Name="Votes" Text="{Binding Question.UpVotes,StringFormat='{0}'}" FontSize="Large" FontAttributes="Bold" />
				<controls:ToggleButton x:Name="UpVoteButton" BindingContext="{Binding}" IsVisible="{Binding IsNewQuestion, Converter={StaticResource Key=cnvInvert}}"/>
			</StackLayout>
			<StackLayout x:Name="BackgroundSegment" Orientation="Vertical" IsVisible="{Binding IsNewQuestion}">
				<Label Text="Background:" FontSize="Medium" HorizontalOptions="Center" />
				<Editor Placeholder="Add Background (optional)" Completed="Background_Entered" Style="{StaticResource NormalEditor}" AutoSize="TextChanges" FontSize="Medium" FontAttributes="Bold" />
			</StackLayout >
            <StackLayout x:Name="LinkOrAnswerSegment" Orientation="Vertical" IsVisible="{Binding IsNewQuestion, Converter={StaticResource Key=cnvInvert}}">
				<Label Text="Link or answer:" />
				<Editor Placeholder="Add an answer or a Hansard link to an answer" Completed="Answer_Entered" Style="{StaticResource NormalEditor}" AutoSize="TextChanges" FontSize="Medium" FontAttributes="Bold" />
			</StackLayout >
            <Button x:Name="SaveAnswerButton" Style="{StaticResource SaveButton}" Text="Save Answer" Clicked="SaveAnswerButton_OnClicked" HorizontalOptions="Center" IsVisible="{Binding IsNewQuestion, Converter={StaticResource Key=cnvInvert}}"/>
			<Button x:Name="SaveBackgroundButton" Style="{StaticResource DoneButton}" Text="Publish New Question!" Clicked="SubmitNewQuestionButton_OnClicked" HorizontalOptions="Center" IsVisible="{Binding IsNewQuestion}"/>
			<Button x:Name="QuestionSuggesterButton" Style="{StaticResource TurquoiseButton}" Clicked="QuestionSuggesterButton_OnClicked" HorizontalOptions="Center"/>
		</StackLayout>
    </ContentPage.Content>
</ContentPage>