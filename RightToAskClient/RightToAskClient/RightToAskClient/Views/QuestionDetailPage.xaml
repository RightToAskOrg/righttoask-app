<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:RightToAskClient.Views;assembly=RightToAskClient"
             xmlns:rightToAskClient="clr-namespace:RightToAskClient;assembly=RightToAskClient"
             xmlns:controls="clr-namespace:RightToAskClient.Controls;assembly=RightToAskClient"
             xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
             xmlns:models="clr-namespace:RightToAskClient.Models;assembly=RightToAskClient"
             xmlns:local="clr-namespace:RightToAskClient"
             xmlns:vm="clr-namespace:RightToAskClient.ViewModels"
             x:Class="RightToAskClient.Views.QuestionDetailPage"
             x:DataType="vm:QuestionViewModel"
             Title="{xct:Translate QuestionDetailsTitle}">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="{xct:Translate HomeButtonText}" Command="{Binding HomeButtonCommand}" x:DataType="vm:BaseViewModel"
                     IconImageSource="{local:ImageResource RightToAskClient.Images.home4.png}"/>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
		<StackLayout x:Name="WholePage" Margin="20">
            <Label Text="{xct:Translate QuestionHeaderText}" Style="{StaticResource LeftSmall}"/>
			<Label Text="{Binding Question.QuestionText}" Style="{StaticResource LeftLarge}" IsVisible="{Binding CanEditQuestion, Converter={StaticResource Key=cnvInvert}}"/>
			<Editor Text="{Binding Question.QuestionText}" Style="{StaticResource NormalEditor}" IsVisible="{Binding CanEditQuestion}"/>
            <Label Text="{xct:Translate QuestionWriterHeaderText}" Style="{StaticResource LeftSmall}"/>
			<Label Text="{Binding Question.QuestionSuggester, StringFormat='{0:F0}'}" Style="{StaticResource LeftMedium}" />
            <Label Text="{xct:Translate UpvotesHeaderText}" Style="{StaticResource LeftSmall}"/>
            <StackLayout  Orientation="Horizontal" HorizontalOptions="FillAndExpand" >
                <Label Text="{Binding Question.UpVotes,StringFormat='{0}'}" Style="{StaticResource LeftLarge}" />
                <Button Text="{Binding UpvoteButtonText}" Style = "{StaticResource GreenButton}" Command="{Binding UpvoteCommand}" IsVisible="{Binding IsNewQuestion, Converter={StaticResource Key=cnvInvert}}"/>
            </StackLayout>
            <StackLayout Orientation="Vertical">
                <Label Text="{xct:Translate BackgroundHeaderText}" Style="{StaticResource LeftSmall}" />
                <Editor Placeholder="{xct:Translate AddBackgroundPlaceholderText}" Text="{Binding Question.Background}" Style="{StaticResource NormalEditor}" IsEnabled="{Binding CanEditBackground}"/>
			</StackLayout >
            <StackLayout Orientation="Vertical" IsVisible="{Binding IsNewQuestion, Converter={StaticResource Key=cnvInvert}}">
                <Label Text="{xct:Translate LinkAnswerHeaderText}" />
                <Editor Placeholder="{xct:Translate LinkAnswerPlaceholderText}" Completed="Answer_Entered" Style="{StaticResource NormalEditor}" />
			</StackLayout >
            <Button Text="{Binding SaveButtonText}" Style="{StaticResource SaveButton}" Command="{Binding SaveAnswerCommand}" HorizontalOptions="Center" IsVisible="{Binding IsNewQuestion, Converter={StaticResource Key=cnvInvert}}"/>
            <Button Text="{xct:Translate EditQuestionButtonText}" Style="{StaticResource SaveButton}" Command="{Binding EditAnswerCommand}" HorizontalOptions="Center" IsVisible="{Binding CanEditQuestion}"/>
            <Button Text="{xct:Translate PublishQuestionButtonText}" Style="{StaticResource DoneButton}" Command="{Binding SaveQuestionCommand}" HorizontalOptions="Center" IsVisible="{Binding IsNewQuestion}"/>
            <Button Text="{Binding QuestionSuggesterButtonText}" Style="{StaticResource GreenButton}" Command="{Binding QuestionSuggesterCommand}" HorizontalOptions="Center" IsVisible="{Binding IsNewQuestion, Converter={StaticResource Key=cnvInvert}}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>