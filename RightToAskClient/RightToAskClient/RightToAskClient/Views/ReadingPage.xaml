<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:controls="clr-namespace:RightToAskClient.Controls;assembly=RightToAskClient"
			 xmlns:rightToAskClient="clr-namespace:RightToAskClient;assembly=RightToAskClient"
             xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
			 xmlns:models="clr-namespace:RightToAskClient.Models;assembly=RightToAskClient"
             xmlns:vm="clr-namespace:RightToAskClient.ViewModels"
			 x:Class="RightToAskClient.Views.ReadingPage"
			 x:Name="TitleBar"
             Title="{xct:Translate RecentQuestionsTitle}">
    <ContentPage.ToolbarItems>
        <ToolbarItem x:Name="HomeButton" Text="{xct:Translate HomeButtonText}"/>
    </ContentPage.ToolbarItems>
    <ContentPage.BindingContext>
        <vm:ReadingPageViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
		<StackLayout x:Name = "WholePage" HorizontalOptions="Center" VerticalOptions="Start">
            <Label Text="{xct:Translate ReadingPageHeader1}" Style="{StaticResource Header2}"></Label>
			<!-- Filters inserted here -->
			<Frame x:Name="FilterShower" Style="{StaticResource TealFrame}">
                <controls:ClickableLabel Tapped="OnShowFilters" Text="{xct:Translate ClickForFiltersText}"/> 
			</Frame>
			<ScrollView Scrolled="Questions_Scrolled">
			<CollectionView x:Name="QuestionList" ItemsSource="{Binding ExistingQuestions}" SelectionMode="Single" SelectedItem="{Binding SelectedQuestion}">
                    <!--HasUnevenRows="True" ItemTapped="Question_Selected"-->
				<CollectionView.ItemTemplate>
					<DataTemplate x:DataType="models:Question">
                        <StackLayout Orientation="Vertical" VerticalOptions="FillAndExpand" Spacing="4">
                            <Label Text="{Binding QuestionText}" Style="{StaticResource ListViewText}"/>
                            <StackLayout Orientation="Horizontal" Padding="0" HorizontalOptions="Start" VerticalOptions="CenterAndExpand">
                                <Label Text="Upvotes " Style="{StaticResource ListViewTextSmall}"/>
                                <Label Text="{Binding UpVotes}" Style="{StaticResource ListViewTextSmall}"/>
                                <!--
								<Button StyleClass="UpVoteButton" Padding="0" Clicked="OnUpVoteButtonClicked" />
								-->
                                <controls:ToggleButton ToBeIncremented="{Binding}"/>
                            </StackLayout>
                        </StackLayout>
                    </DataTemplate>
				</CollectionView.ItemTemplate>
			</CollectionView>
			</ScrollView>	
            <StackLayout Orientation="Horizontal">
                <Editor Placeholder="{xct:Translate DraftQuestionPlaceholderText}" Text="{Binding DraftQuestion}" 
                        HorizontalOptions="FillAndExpand" Style="{StaticResource NormalEditor}"
                        IsVisible="{Binding ShowDraftEditor}"/>
                <!--Completed="Question_Entered"-->
            </StackLayout>
			<StackLayout Orientation="Horizontal">
                <Button Text="{xct:Translate DiscardDraftButtonText}" Style="{StaticResource TurquoiseButton}" 
                        Command="{Binding DiscardButtonCommand}" IsVisible="{Binding ShowDiscardButton}"/>
                <Button Text="{xct:Translate KeepQuestionButtonText}" Style="{StaticResource DoneButton}" 
                        Command="{Binding KeepQuestionButtonCommand}" IsVisible="{Binding ShowContinueButton}" />
			</StackLayout>
		</StackLayout>
	</ContentPage.Content>
</ContentPage>
