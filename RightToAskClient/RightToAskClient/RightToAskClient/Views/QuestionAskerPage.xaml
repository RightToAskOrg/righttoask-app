<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:rightToAskClient="clr-namespace:RightToAskClient;assembly=RightToAskClient"
             xmlns:controls="clr-namespace:RightToAskClient.Controls;assembly=RightToAskClient"
             xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
             xmlns:local="clr-namespace:RightToAskClient"
             xmlns:vm="clr-namespace:RightToAskClient.ViewModels"
             x:Class="RightToAskClient.Views.QuestionAskerPage"
             Title="{xct:Translate RaiseQuestionTitle}"
             x:DataType="vm:QuestionViewModel">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="{xct:Translate HomeButtonText}" Command="{Binding HomeButtonCommand}" x:DataType="vm:BaseViewModel"
                     IconImageSource="{local:ImageResource RightToAskClient.Images.home1.png}"/>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout x:Name = "WholePage">
			<StackLayout x:Name="questionAsker" VerticalOptions="FillAndExpand" Padding="10">
                <Label Text="{xct:Translate RaiseQuestionHeaderText}" Style="{StaticResource Header2}" HorizontalTextAlignment="Center" VerticalOptions="Start"/>
                <Grid RowDefinitions="*,*,auto" ColumnDefinitions="*,*" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
                    <Frame Grid.Row="0" Grid.Column="0" Style="{StaticResource InvisibleButtonFrame}">
                        <Grid RowDefinitions="*,*" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
                            <Label Grid.Row="0" Text="{xct:Translate FindCommitteeButtonText}" Style="{StaticResource HeaderButtonText}"/>
                            <Label Grid.Row="1" Text="A Parliamentary Committee, such as Senate Estimates" Style="{StaticResource BodyButtonText}"/>
                            <Button Grid.RowSpan="2" Style="{StaticResource TransparentSquareButton}" 
                                Command="{Binding SelectCommitteeButtonCommand}" IsVisible="{Binding DisplayFindCommitteeButton}"/>
                        </Grid>
                    </Frame>
                    <Frame Grid.Row="0" Grid.Column="1" Style="{StaticResource InvisibleButtonFrame}">
                        <Grid RowDefinitions="*,*">
                            <Label Grid.Row="0" Text="{xct:Translate MyMPButtonText}" Style="{StaticResource HeaderButtonText}"/>
                            <Label Grid.Row="1" Text="Your MP, perhaps a special session for constituent questions" Style="{StaticResource BodyButtonText}"/>
                            <Button Grid.RowSpan="2" Style="{StaticResource TransparentSquareButton}" 
                                Command="{Binding RaisedOptionSelectedCommand}" CommandParameter="1" IsVisible="{Binding DisplayFindCommitteeButton}"/>
                        </Grid>
                    </Frame>
                    <Frame Grid.Row="1" Grid.Column="0" Style="{StaticResource InvisibleButtonFrame}">
                        <Grid RowDefinitions="*,*">
                            <Label Grid.Row="0" Text="{xct:Translate AnotherMPButtonText}" Style="{StaticResource HeaderButtonText}"/>
                            <Label Grid.Row="1" Text="Blank here." Style="{StaticResource BodyButtonText}"/>
                            <Button Grid.RowSpan="2" Style="{StaticResource TransparentSquareButton}" 
                                Command="{Binding RaisedOptionSelectedCommand}" CommandParameter="2" IsVisible="{Binding DisplayFindCommitteeButton}"/>
                        </Grid>
                    </Frame>
                    <Frame Grid.Row="1" Grid.Column="1" Style="{StaticResource InvisibleButtonFrame}">
                        <Grid RowDefinitions="*,*">
                            <Label Grid.Row="0" Text="{xct:Translate AnotherUserButtonText}" Style="{StaticResource HeaderButtonText}"/>
                            <Label Grid.Row="1" Text="Blank here." Style="{StaticResource BodyButtonText}"/>
                            <Button Grid.RowSpan="2" Style="{StaticResource TransparentSquareButton}" 
                                Command="{Binding RaisedOptionSelectedCommand}" CommandParameter="3" IsVisible="{Binding DisplayFindCommitteeButton}"/>
                        </Grid>
                    </Frame>
                    <Frame Grid.Row="2" Grid.ColumnSpan="2" Style="{StaticResource InvisibleButtonFrame}">
                        <Grid RowDefinitions="*,*">
                            <Label Grid.Row="0" Text="{xct:Translate NotSureButtonText}" Style="{StaticResource HeaderButtonText}"/>
                            <Label Grid.Row="1" Text="{xct:Translate Step2BodyText7}" Style="{StaticResource BodyButtonText}"/>
                            <Button Grid.RowSpan="2" Style="{StaticResource TransparentSquareButton}" 
                                Command="{Binding RaisedOptionSelectedCommand}" CommandParameter="4" IsVisible="{Binding DisplayFindCommitteeButton}"/>
                        </Grid>
                    </Frame>
                </Grid>
                <!--
                <StackLayout Orientation="Horizontal" IsVisible="{Binding DisplaySenateEstimatesSection}">
                    <Label Text="{Binding SenateEstimatesAppearanceText}" Style="{StaticResource Header2}" />
					<Button Style="{StaticResource GreenButton}" Text="{Binding SelectButtonText}" Command="{Binding SelectCommitteeButtonCommand}" />
				</StackLayout>
                <Label Text="{xct:Translate Step2BodyText4}" Style="{StaticResource Header2}" />
                <Button Text="{xct:Translate MyMPButtonText}" Style="{StaticResource GreenButton}" Command="{Binding RaisedOptionSelectedCommand}" CommandParameter="1" IsEnabled="{Binding MPButtonsEnabled}"/>
                <Label Text="{xct:Translate Step2BodyText5}" Style="{StaticResource Header2}" />
                <Button Text="{xct:Translate AnotherMPButtonText}" Style="{StaticResource GreenButton}" Command="{Binding RaisedOptionSelectedCommand}" CommandParameter="2" IsEnabled="{Binding MPButtonsEnabled}"/>
                <Label Text="{xct:Translate Step2BodyText6}" Style="{StaticResource Header2}" />
                <Button Text="{Binding AnotherUserButtonText}" Style="{StaticResource GreenButton}" Command="{Binding RaisedOptionSelectedCommand}" CommandParameter="3"/>
                <Label Text="{xct:Translate Step2BodyText7}" Style="{StaticResource Header2}" />
                <Button Text="{Binding NotSureWhoShouldRaiseButtonText}" Style="{StaticResource GreenButton}" Command="{Binding RaisedOptionSelectedCommand}" CommandParameter="4" />
				<Label Text="{Binding ReportLabelText}" IsVisible="{Binding ShowReportLabel}"/>-->
            </StackLayout>
            <!--<Button Text="{xct:Translate NextButtonText}" Style="{StaticResource DoneButton}" Command="{Binding ProceedToReadingPageCommand}" IsVisible="{Binding RaisedByOptionSelected}"/>-->
        </StackLayout>
    </ContentPage.Content>
</ContentPage>