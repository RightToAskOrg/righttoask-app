<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:rightToAskClient="clr-namespace:RightToAskClient;assembly=RightToAskClient"
             xmlns:controls="clr-namespace:RightToAskClient.Controls;assembly=RightToAskClient"
             xmlns:vm="clr-namespace:RightToAskClient.ViewModels"
             x:Class="RightToAskClient.Views.QuestionAskerPage"
             x:DataType="vm:QuestionViewModel">
    <ContentPage.Resources>
        <Style x:Key="TurquoiseButton" TargetType="Button">
            <Setter Property="CornerRadius" Value="20"/>
            <Setter Property="BackgroundColor" Value="Turquoise"/>
            <Setter Property="HeightRequest" Value="40"/>
        </Style>
    </ContentPage.Resources>
    <ContentPage.Content>
        <StackLayout x:Name = "WholePage">
			<StackLayout x:Name="questionAsker" Orientation="Vertical">
				<Label Text="Now we'll help you direct your question to where it might get answered" FontSize="Medium" HorizontalOptions="Center" />
				<Label Text="Should it be raised by... " FontSize="Medium" HorizontalOptions="Center" />
				<Label Text="a Parliamentary Committee, such as Senate Estimates?" FontSize="Medium" HorizontalOptions="Center" />
                <Button Style="{StaticResource TurquoiseButton}" Text="Find a Committee" Command="{Binding RaisedOptionSelectedCommand}" CommandParameter="0" IsVisible="{Binding DisplayFindCommitteeButton}"/>
                <StackLayout Orientation="Horizontal" IsVisible="{Binding DisplaySenateEstimatesSection}">
					<Label Text="{Binding SenateEstimatesAppearanceText}" FontSize="Medium" HorizontalOptions="Center" />
					<Button Style="{StaticResource TurquoiseButton}" Text="{Binding SelectButtonText}" Command="{Binding SelectCommitteeButtonCommand}" />
				</StackLayout>
				<Label Text="or by your MP, perhaps in a special session for constituent questions?" FontSize="Medium" HorizontalOptions="Center" />
                <Button Style="{StaticResource TurquoiseButton}" Text="My MP" Command="{Binding RaisedOptionSelectedCommand}" CommandParameter="1" IsEnabled="{Binding MPButtonsEnabled}"/>
				<Label Text="or by some other MP?" FontSize="Medium" HorizontalOptions="Center" />
                <Button Style="{StaticResource TurquoiseButton}" Text = "Another MP" Command="{Binding RaisedOptionSelectedCommand}" CommandParameter="2" IsEnabled="{Binding MPButtonsEnabled}"/> 
				<Label Text="a journalist or other RightToAsk participant?" FontSize="Medium" HorizontalOptions="Center" />
                <Button Style="{StaticResource TurquoiseButton}" Text="{Binding AnotherUserButtonText}" Command="{Binding RaisedOptionSelectedCommand}" CommandParameter="3"/>
				<Label Text="or are you just not sure?" FontSize="Medium" HorizontalOptions="Center" />
                <Button Style="{StaticResource TurquoiseButton}" Text="{Binding NotSureWhoShouldRaiseButtonText}" Command="{Binding RaisedOptionSelectedCommand}" CommandParameter="4" />
				<Label Text="{Binding ReportLabelText}" IsVisible="{Binding ShowReportLabel}"/>
            </StackLayout>
            <Button Text="Next" StyleClass="DoneButton" Command="{Binding NavigateForwardCommand}" IsVisible="{Binding RaisedByOptionSelected}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>