namespace RightToAskClient.Maui.Models.ServerCommsData
{
    /* The data structure for receiving MiniUserInfo and error message associated with a
     * search for users matching a certain string.
     * The server data structure returns only MiniUserInfo, which includes only uid, display_name and badges,
     * but we pull into a Regsitration data structure which has a superset of those fields.
     */
    public class UserSearchResults
    {
        
    }
}